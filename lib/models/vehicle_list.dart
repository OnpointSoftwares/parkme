class VehicleData {
  var id;
  String title;
  String url;
  String thumbnail;
  String owner;
  String vehicleNumber;

  VehicleData({required this.id,required this.title,required this.url,required this.thumbnail,required this.owner,required this.vehicleNumber});
}